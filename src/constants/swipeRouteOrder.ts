/**
 * Route order for `useSwipeNavigation` (left/right swipe and horizontal wheel).
 * Keep in sync with `<Routes>` in App.tsx.
 */
export const SWIPE_ROUTE_ORDER = [
  '/',
  '/about',
  '/experiences',
  '/projects',
  '/contact',
] as const;
